Module 'AnotherModule' defines an attribute a

    class AnotherModule(Module):
        a: SomeProvider.a

It both doesn't have a value and points to a provider's resource. It's likely
that you intended to define instead:

    class AnotherModule(Module):
        a = Resource(int)

See wiring/module/test_module.py:160
