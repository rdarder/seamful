Module 'AnotherModule' defines an attribute 'b'

    class AnotherModule(Module):
        b: SomeModule.a

It both doesn't have a value and points to another module's resource. It's
likely that you intended to define instead:

    class AnotherModule(Module):
        b = Resource(int)

See wiring/module/test_module.py:195
