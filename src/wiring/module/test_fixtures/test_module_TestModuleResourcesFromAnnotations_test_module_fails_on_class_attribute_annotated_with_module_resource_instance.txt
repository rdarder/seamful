Module 'SomeModule' defines an attribute 'a'

    class SomeModule(Module):
        a: Resource(int)

But it has no value. It's likely that you intended to define instead:

    class SomeModule(Module):
        a = Resource(int)

SomeModule: src/wiring/module/test_module.py:137
