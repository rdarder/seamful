Module 'SomeModule' defines an attribute:
    class SomeModule(Module):
        a: Resource(int)

But it has no value. It's likely that you intended to define instead:
    class SomeModule(Module):
        a = Resource(int)

See wiring/module/test_module.py:121
