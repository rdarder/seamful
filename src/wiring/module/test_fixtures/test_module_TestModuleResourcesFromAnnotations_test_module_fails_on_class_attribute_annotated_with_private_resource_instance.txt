Module 'SomeModule' defines an attribute 'a'

    class SomeModule(Module):
        a: Resource(int, private=True)

But it has no value, plus module resources cannot be private. It's likely that
you intended to define instead:

    class SomeModule(Module):
        a = Resource(int)

See wiring/module/test_module.py:142
