Module 'SomeModule' defines an attribute 'a'

    class SomeModule(Module):
        a: Resource(int, override=True)

But it has no value, plus module resources cannot be overrides. It's likely that
you intended to define instead:

    class SomeModule(Module):
        a = Resource(int)

SomeModule: src/wiring/module/test_module.py:212
