Provider 'SomeProvider' provides for 'SomeModule', but it's missing a provider
method for resource:

    SomeProvider.a = Resource(ConcreteClass, override=True)

Providers for 'SomeModule' must have a provider method  for each of its
resources.
